# =============================================================================
# JOBS D'APPLICATION TERRAFORM (VERSION REFACTORISÉE)
# =============================================================================

# Application en environnement dev
apply_dev:
  extends: .terraform_apply_dev
  dependencies:
    - plan_dev

# Application en environnement prod
apply_prod:
  extends: .terraform_apply_prod
  dependencies:
    - plan_prod