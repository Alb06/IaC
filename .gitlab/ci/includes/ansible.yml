# =============================================================================
# JOBS ANSIBLE GITLAB CI/CD
# =============================================================================
# Description : Jobs spécifiques pour les déploiements Ansible
# Version     : 1.0.0
# =============================================================================

# Job de test de connectivité Ansible
ansible_ping:
  extends: 
    - .ansible_base
    - .setup_ssh
  stage: validate
  script:
    - echo "🔍 Test de connectivité Ansible..."
    - |
      for env in dev prod; do
        if [ -f "ansible/inventory/$env" ]; then
          echo "Test environnement: $env"
          ansible all -i "ansible/inventory/$env" -m ping
        fi
      done
  rules:
    - if: '$CI_PIPELINE_SOURCE == "merge_request_event"'
      changes:
        - ansible/**/*
    - if: '$CI_COMMIT_BRANCH == "main"'
      changes:
        - ansible/**/*

# Template pour les déploiements Ansible
.ansible_deploy:
  extends:
    - .ansible_base
    - .setup_ssh
  stage: deploy
  script:
    - cd ${CI_PROJECT_DIR}/ansible
    - |
      # Vérification de l'inventaire
      inventory_file="inventory/${ENV}"
      if [ ! -f "$inventory_file" ]; then
        echo "❌ Inventaire $inventory_file non trouvé"
        exit 1
      fi
      
      echo "📋 Inventaire utilisé: $inventory_file"
      ansible-inventory -i "$inventory_file" --list
      
    - |
      # Exécution du playbook principal
      if [ -f "playbooks/${PLAYBOOK:-setup.yml}" ]; then
        echo "🚀 Exécution du playbook ${PLAYBOOK:-setup.yml}..."
        ansible-playbook -i "$inventory_file" \
          "playbooks/${PLAYBOOK:-setup.yml}" \
          ${ANSIBLE_EXTRA_VARS:+-e "$ANSIBLE_EXTRA_VARS"} \
          ${DRY_RUN:+--check --diff}
      else
        echo "❌ Playbook ${PLAYBOOK:-setup.yml} non trouvé"
        exit 1
      fi

# Déploiement Ansible Dev
ansible_deploy_dev:
  extends:
    - .ansible_deploy
    - .dev_environment
  needs:
    - job: apply_dev
      artifacts: true
  variables:
    PLAYBOOK: setup.yml
    DRY_RUN: "true"  # Mode check par défaut en dev

# Déploiement Ansible Prod
ansible_deploy_prod:
  extends:
    - .ansible_deploy
    - .prod_environment
  needs:
    - job: apply_prod
      artifacts: true
  variables:
    PLAYBOOK: setup.yml
    ANSIBLE_EXTRA_VARS: "deployment_type=production"